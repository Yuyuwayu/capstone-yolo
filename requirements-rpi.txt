# Raspberry Pi application dependencies.
#
# Install binary packages from apt first:
# sudo apt install python3-numpy python3-opencv python3-scipy \
#   python3-matplotlib python3-torch python3-torchvision
#
# Install Ultralytics separately without dependencies:
# pip install --no-cache-dir --no-deps ultralytics

fastapi
uvicorn
python-multipart
Pillow
PyYAML
requests
psutil
packaging
tqdm
